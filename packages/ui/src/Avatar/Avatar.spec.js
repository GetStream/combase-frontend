import React from "react";

import { render } from '../../../../config/test-utils';

import { Avatar } from ".";

describe("Avatar", () => {
  test("renders correctly", () => {
    render(<Avatar />);
  });
});
