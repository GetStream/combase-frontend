import React from "react";

import { render } from '../../../../config/test-utils';

import { Entity } from ".";

describe("Entity", () => {  
  test("renders correctly", () => {
    render(<Entity />);
  });
});
