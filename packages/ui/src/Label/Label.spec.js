import React from "react";

import { render } from '../../../../config/test-utils';

import { Label } from ".";

describe("Label", () => {  
  test("renders correctly", () => {
    render(<Label />);
  });
});
