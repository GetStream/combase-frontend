import React from 'react';

import PageTitle from './PageTitle';

export const Default = () => (
    <PageTitle subtitle="Stream" title="Conversations" />
);

export default {
    component: PageTitle,
    title: "shared/PageTitle",
};