import React from 'react';

import { render } from '@conf/test-utils';

import Spinner from '.';

describe('Spinner', () => {
    test('renders correctly', () => {
        render(<Spinner />);
    });
});
