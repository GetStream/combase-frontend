import React from 'react';

import { render } from '@conf/test-utils';

import { Checkbox } from '.';

describe('Checkbox', () => {
    test('renders correctly', () => {
        render(<Checkbox />);
    });
});
