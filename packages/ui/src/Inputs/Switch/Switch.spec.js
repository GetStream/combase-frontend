import React from 'react';

import { render } from '@conf/test-utils';

import { Switch } from '.';

describe('Switch', () => {
    test('renders correctly', () => {
        render(<Switch />);
    });
});
