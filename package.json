{
  "name": "combase",
  "license": "MIT",
  "private": true,
  "scripts": {
	"build:web": "yarn workspace dashboard build",
	"build:widget": "yarn workspace @combase.app/widget build",
	"build:widget-playground": "yarn workspace widget-playground build",
	"start:web": "yarn workspace dashboard dev",
	"start:widget-playground": "yarn workspace widget-playground dev",
	"storybook": "yarn workspace storybook storybook"
  },
  "workspaces": {
	"packages": [
		"packages/*",
		"storybook"
	]
  }
}
